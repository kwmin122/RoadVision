# opencv-contrib-python은 opencv-python의 상위집합이다.
# --vehicle-track 모드(L10 CSRT 추적)를 위해 opencv-contrib로 교체함.
# opencv-python과 opencv-contrib-python을 동시에 설치하면 충돌 → 반드시 contrib만 설치.
opencv-contrib-python>=4.13
numpy>=1.24
Pillow>=10.0          # 한글 텍스트 렌더링 (PIL.ImageFont.truetype + ImageDraw)
